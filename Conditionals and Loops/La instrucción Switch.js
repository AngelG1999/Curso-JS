/* 

SINTAXIS

switch (expression) {
  case n1: 
     statements
     break;
  case n2: 
     statements
     break;
  default: 
     statements
     
     */

/*
var day = 2;
switch (day) {
  case 1:
    document.write("Monday");
    break;
  case 2:
    document.write("Tuesday");
    break;
  case 3:
    document.write("Wednesday");
    break;
  default:
    document.write("Another day");
}
*/

    /* 

    BREAK

Cuando el código JavaScript alcanza una palabra clave de dreak , se sale del bloque de interruptores.

    */

    /*
    
    DEFAULT
    
    La palabra clave default especifica el código a ejecutar si no hay coincidencia de mayúsculas y minúsculas.

    var color = " amarillo "; 
switch (color) { 
   case "blue": 
     document.write ("Esto es azul"); 
     break; 
   case "red": 
     document.write ("Esto es rojo"); 
     break; 
   case "green": 
     document.write ("Esto es verde"); 
     break; 
   case "orange": 
      document.write ("Esto es naranja"); 
      break; 
   default: 
      document.write ("Color no encontrado"); 
} 

// Salidas "Color no encontrado".

    */